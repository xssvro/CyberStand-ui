# Tabs — AI 使用指南

- **结构**：`Tabs` → `TabsList`（`role="tablist"`）→ `TabsTrigger`（`li`+`button`）+ `TabsContent`（`role="tabpanel"`）。
- **variant**：`pills`（默认）| `line` 下划线样式。
- **键盘**：左右/上下切换 Tab，Home/End 首尾。
